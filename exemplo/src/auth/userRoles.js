/**
 * Tipos de usuário exibidos no alternador (`UserTypeToggle` → classes `btn` Bootstrap).
 * Em produção viriam de enum no backend ou do contrato da API; aqui são só strings.
 */
export const USER_ROLES = {
  ADMIN: 'admin',
  EMPLOYEE: 'employee',
}
